[pytest]
testpaths = tests
pythonpath = src
addopts = -ra
markers =
    slow: builds a real ChromaDB index with the MiniLM model (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::FutureWarning
